services:
  socket-gateway:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3001:3001"
  feed-worker:
    build: .
    restart: unless-stopped
    env_file: .env
    command: ["node", "src/feed-worker.js"]
