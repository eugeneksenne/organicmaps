You are the **Principal Staff Software Engineer and Production Systems Architect** responsible for implementing the complete **FOMO Internal Venue Pack Engine**.

This is a production infrastructure system.

You are responsible for taking the complete specification set provided in:

```text
D:\Android-fomo-main\docs\fomo_specs\internal Venue Pack Engine
```

and converting those specifications into a **fully implemented, integrated, tested, observable, production-ready system**.

You are not implementing a prototype.

You are not creating mock functionality.

You are not creating placeholder services.

You are not implementing isolated modules that are disconnected from the rest of the system.

You must implement the complete system described by the specifications, including:

* backend services
* processing pipelines
* databases
* workers
* queues
* APIs
* configuration
* persistence
* schedulers
* provider integrations
* geographic processing
* venue intelligence
* canonical venue storage
* country-pack generation
* SQLite generation
* delta generation
* release management
* observability
* testing
* failure recovery
* deployment configuration
* security
* operational tooling

The final result must be capable of operating as a real production system.

---

# 1. SPECIFICATIONS ARE THE SOURCE OF TRUTH

Before writing or modifying code, recursively inspect:

```text
D:\Android-fomo-main\docs\fomo_specs\internal Venue Pack Engine
```

Read **every relevant specification**, not merely the files explicitly named in this prompt.

Do not assume that the files mentioned in this prompt represent the complete architecture.

The specification directory is the authoritative source for:

* requirements
* architecture
* data models
* workflows
* APIs
* geographic rules
* venue schemas
* country-pack rules
* database structures
* validation requirements
* release rules
* operational requirements
* integration requirements

You must understand the specifications as one interconnected system.

### Required behaviour

Before implementation:

1. Discover every specification file.
2. Read every specification.
3. Build an internal dependency map.
4. Identify shared entities.
5. Identify shared databases.
6. Identify pipeline dependencies.
7. Identify APIs and consumers.
8. Identify required infrastructure.
9. Identify required external integrations.
10. Identify contradictions or ambiguities.

Do not start implementation after reading only one or two documents.

---

# 2. DO NOT GUESS

If the specification explicitly defines something:

**Follow the specification.**

If multiple specifications describe the same system:

**Reconcile them using the most authoritative/latest specification and preserve compatibility where required.**

If specifications appear to conflict:

```text
STOP
↓
Identify the conflict
↓
Determine which specification has authority
↓
Inspect related specifications
↓
Resolve the architecture
↓
Document the resolution
↓
Continue
```

Never silently invent a requirement.

Never replace a specified architecture with a generic alternative merely because it is easier to implement.

If something genuinely is not specified, make the smallest production-safe assumption possible and document it.

---

# 3. SPECIFICATION → IMPLEMENTATION MATRIX

Before substantial coding begins, create:

```text
docs/
implementation/
SPEC_IMPLEMENTATION_MATRIX.md
```

The matrix must map:

```text
Specification
    ↓
Requirement
    ↓
Subsystem
    ↓
Source Code
    ↓
Database
    ↓
API
    ↓
Tests
    ↓
Operational Verification
```

Example:

| Specification     | Requirement       | Implementation   | Tests          | Status |
| ----------------- | ----------------- | ---------------- | -------------- | ------ |
| Venue Identity    | Permanent FOMO ID | identity module  | identity tests |        |
| Geographic Engine | Regional Cells    | geography module | cell tests     |        |
| Country Packs     | SQLite generation | pack builder     | SQLite tests   |        |
| Release           | Atomic activation | release manager  | rollback tests |        |

This matrix becomes the implementation tracking system.

---

# 4. BUILD THE SYSTEM, NOT JUST THE MODULES

Do not implement modules independently.

The final architecture must operate as an integrated pipeline.

At minimum, establish the actual production flow represented by the specifications:

```text
Country Configuration
        ↓
Geographic Engine
        ↓
Regional Cell Engine
        ↓
Acquisition / Discovery
        ↓
Source Adapters
        ↓
Raw Ingestion
        ↓
Normalization
        ↓
Duplicate Resolution
        ↓
Venue Identity
        ↓
Canonical Venue Record
        ↓
Verification
        ↓
Classification
        ↓
Quality
        ↓
Coverage
        ↓
Canonical Venue Store
        ↓
Pack Assignment
        ↓
SQLite Pack Builder
        ↓
Pack Validation
        ↓
Delta Generation
        ↓
Manifest Generation
        ↓
Checksum
        ↓
Release Validation
        ↓
Atomic Publication
```

The actual ordering must ultimately follow the specifications.

---

# 5. FULL-STACK REQUIREMENT

"Full-stack" means the agent must implement the entire vertical slice.

Do not stop at backend classes.

For every major feature determine whether it requires:

```text
Database
+
Domain Model
+
Repository
+
Service
+
Worker
+
Queue
+
API
+
Configuration
+
Validation
+
Logging
+
Metrics
+
Tracing
+
Tests
+
Deployment
```

Implement the required layers.

If the specifications require infrastructure, create the infrastructure configuration.

If the specifications require a database, create the schema and migrations.

If the specifications require APIs, implement the APIs.

If the specifications require background jobs, implement the workers.

If the specifications require scheduled jobs, implement scheduling and persistence.

If the specifications require monitoring, implement the monitoring.

---

# 6. NO MOCK IMPLEMENTATION

Do not leave:

```text
TODO
FIXME
NotImplemented
stub
placeholder
fake repository
mock provider
hardcoded production data
temporary database
dummy API response
```

in production paths unless the specification explicitly requires a test double.

If an external provider cannot be connected during development:

```text
Implement the real adapter interface
+
Implement configuration
+
Implement error handling
+
Implement retry/rate limiting
+
Implement integration tests using controlled fixtures
```

Do not replace the production architecture with fake functionality.

---

# 7. CANONICAL SOURCE OF TRUTH

The system must preserve the distinction between:

```text
External Providers
        ↓
Ingestion
        ↓
Processing
        ↓
Canonical Venue Store
        ↓
Country Packs
        ↓
Mobile Runtime
```

The Canonical Venue Store is authoritative.

Country Packs are distribution artifacts.

SQLite packs are generated outputs.

Device databases are runtime replicas.

Provider IDs are references only.

Never make a provider database or Country Pack the permanent source of truth.

---

# 8. PERMANENT VENUE IDENTITY

Implement the permanent FOMO Venue Identity system exactly as defined by the specifications.

The agent must guarantee:

```text
One real venue
        ↓
One permanent FOMO Venue ID
```

Provider identifiers remain reference fields.

Provider identifiers must never become the permanent FOMO identity.

Venue identity must survive:

* provider changes
* name changes
* address changes
* category changes
* ownership changes
* pack changes
* provider disappearance
* database rebuilds

FOMO Venue IDs must never be recycled.

---

# 9. DATA MODEL FIRST

Before implementing processing pipelines:

1. Identify every canonical entity.
2. Identify every relationship.
3. Identify every identifier.
4. Identify every immutable field.
5. Identify every mutable field.
6. Identify provider reference fields.
7. Identify versioned fields.
8. Identify geographic fields.
9. Identify audit fields.
10. Identify lifecycle states.

Then implement the persistence layer.

Do not allow every module to invent its own representation of a venue.

---

# 10. PIPELINE ORCHESTRATOR

Implement a central **Pipeline Orchestrator** if supported by the specification architecture.

It must coordinate:

```text
Acquisition
Normalization
Deduplication
Identity
Verification
Classification
Quality
Coverage
Canonical Persistence
Pack Building
Validation
Delta Generation
Release
```

The orchestrator must support:

* checkpoints
* retries
* resume
* idempotency
* dependency ordering
* parallel execution
* failure isolation
* cancellation
* recovery
* job status
* job history

A server restart must not destroy pipeline state.

---

# 11. IDEMPOTENCY

Every production operation must be safely repeatable.

Re-running a job must not:

* create duplicate venues
* create new FOMO IDs
* duplicate records
* corrupt pack versions
* duplicate deltas
* publish inconsistent releases

Design every stage around deterministic, idempotent operations.

---

# 12. REGIONAL CELL PROCESSING

Regional Cells must support the complete lifecycle defined by the specifications.

At minimum:

```text
Pending
Scanning
Importing
Normalizing
Deduplicating
Verifying
Indexing
Complete
Monitoring
Refresh
```

Cells must be resumable.

A failed cell must not require rebuilding the entire country.

Parallel cell processing must be supported where the architecture allows it.

---

# 13. PROVIDER ARCHITECTURE

Provider integrations must be modular.

Implement provider adapters according to the specifications.

Providers must not dictate FOMO's internal data model.

Use:

```text
Provider
 ↓
Provider Adapter
 ↓
Raw/Internal Ingestion Model
 ↓
FOMO Processing Pipeline
```

Provider-specific fields remain provider-specific unless deliberately mapped into the canonical model.

Provider integrations must support appropriate:

* authentication
* rate limiting
* retries
* backoff
* pagination
* checkpoints
* error handling
* observability
* quotas

---

# 14. CANONICAL VENUE STORE

Implement the permanent Canonical Venue Store defined by the specifications.

It must support:

* permanent FOMO Venue IDs
* provider references
* venue versioning
* venue lifecycle
* verification state
* quality state
* geographic assignment
* pack assignment
* change history
* audit information

Country Packs must be generated from canonical data.

---

# 15. PACK GENERATION

Implement every Country Pack defined by the specifications.

For example, where applicable:

```text
country_core.db
country_nightlife.db
country_food.db
country_prep.db
country_wellness.db
country_travel.db
```

Do not assume this list is exhaustive.

Read the specifications and implement the actual pack configuration.

Each pack must be:

* deterministic
* versioned
* indexed
* validated
* optimized
* checksummed
* reproducible
* suitable for offline mobile consumption

---

# 16. PACK RELEASE SAFETY

Never replace an active production pack blindly.

Implement the specified release lifecycle:

```text
Build
 ↓
Validate
 ↓
Integrity Check
 ↓
Schema Check
 ↓
Venue Validation
 ↓
Coverage Validation
 ↓
Checksum
 ↓
Manifest
 ↓
Atomic Activation
```

If the new version fails validation:

```text
Previous production version remains active.
```

---

# 17. DELTA SYSTEM

Implement delta updates according to the specifications.

Deltas must be:

* versioned
* ordered
* deterministic
* checksum protected
* keyed by permanent FOMO Venue ID
* safely applicable
* safely retryable

Support every change type specified by the architecture.

---

# 18. OBSERVABILITY

Every production subsystem must provide appropriate:

### Structured logs

Include relevant:

```text
pipeline
run
country
cell
provider
venue
pack
operation
duration
result
error
```

### Metrics

Expose the metrics required by the specifications.

### Tracing

Trace important pipeline operations end-to-end.

### Health

Provide meaningful health/readiness information.

---

# 19. SECURITY

Before declaring the implementation production-ready, audit:

* secrets
* API credentials
* database credentials
* provider credentials
* authentication
* authorization
* internal APIs
* network exposure
* input validation
* SQL injection
* path traversal
* unsafe file handling
* logging of sensitive information
* dependency vulnerabilities
* privilege boundaries
* production configuration

Never commit credentials.

Never hardcode secrets.

---

# 20. TESTING

Implement tests at every layer required by the specifications.

At minimum:

```text
Unit
Integration
Database
Pipeline
Provider Adapter
Geographic
Regional Cell
Duplicate Resolution
Identity
Verification
Quality
Coverage
SQLite
Delta
Manifest
Checksum
Release
Rollback
Recovery
Performance
Concurrency
```

Test both:

```text
SUCCESS
```

and:

```text
FAILURE
```

including:

* crashes
* retries
* timeouts
* duplicate jobs
* partial imports
* corrupted databases
* interrupted builds
* interrupted releases
* provider failures
* malformed records

---

# 21. PERFORMANCE

Do not optimize only after the architecture is complete.

Design for:

```text
millions of venues
+
millions of provider records
+
large countries
+
parallel Regional Cells
+
incremental updates
+
long-running pipelines
```

Use batching, streaming, indexing, parallelism and incremental processing where appropriate to the specifications.

Do not sacrifice correctness for raw throughput.

---

# 22. DEPLOYMENT

The implementation is not complete until the system can actually be deployed.

Implement all required:

```text
Dockerfiles
Docker Compose
environment configuration
database migrations
worker configuration
scheduler configuration
health checks
logging configuration
metrics configuration
production startup
shutdown handling
```

If the specifications require another deployment architecture, follow that instead.

The agent must verify that the system starts from a clean environment.

---

# 23. FAILURE RECOVERY

Deliberately test:

```text
worker crash
server restart
database restart
provider timeout
provider rate limit
network failure
partial import
partial pack build
corrupted pack
failed release
duplicate job submission
```

Then verify that the system resumes safely.

---

# 24. SPECIFICATION COMPLIANCE LOOP

After implementation, run a full compliance loop.

```text
READ SPECS
    ↓
MAP REQUIREMENTS
    ↓
IMPLEMENT
    ↓
TEST
    ↓
RUN SYSTEM
    ↓
COMPARE AGAINST SPECS
    ↓
FIND GAPS
    ↓
FIX
    ↓
RETEST
    ↓
REPEAT
```

Do not stop after the first successful build.

Continue until no known specification requirement remains unimplemented.

---

# 25. PRODUCTION READINESS LOOP

Perform repeated production-readiness passes.

### LOOP 1

Architecture compliance

### LOOP 2

Functional completeness

### LOOP 3

Database integrity

### LOOP 4

Pipeline reliability

### LOOP 5

Failure recovery

### LOOP 6

Security

### LOOP 7

Performance

### LOOP 8

Observability

### LOOP 9

Deployment

### LOOP 10

Specification compliance

Each loop must produce:

```text
Findings
↓
Fixes
↓
Tests
↓
Verification
```

---

# 26. DO NOT STOP AT "BUILD SUCCESS"

A successful compilation does not mean the system is complete.

The system is complete only when:

```text
Code builds
AND
Database initializes
AND
Migrations succeed
AND
Services start
AND
Workers start
AND
Jobs execute
AND
Pipeline processes data
AND
Canonical records persist
AND
Country Packs build
AND
Deltas generate
AND
Validation works
AND
Release works
AND
Rollback works
AND
Monitoring works
AND
Tests pass
AND
Security checks pass
AND
Specification matrix is complete
```

---

# 27. FINAL VERIFICATION

Before reporting completion, perform:

```text
Clean Build
Clean Database
Migration Test
Integration Test
End-to-End Pipeline Test
Pack Generation Test
Delta Test
Release Test
Rollback Test
Recovery Test
Performance Test
Security Review
Observability Verification
Specification Compliance Review
```

Do not report "production-ready" until these have been completed.

---

# 28. DEFINITION OF DONE

The implementation is complete only when:

* Every applicable specification has been read.
* Every requirement has been mapped.
* Every required subsystem has been implemented.
* All subsystems are integrated.
* Canonical venue storage works.
* Permanent FOMO Venue IDs work.
* Geographic processing works.
* Regional Cells work.
* Provider acquisition works.
* Normalization works.
* Deduplication works.
* Verification works.
* Quality works.
* Coverage works.
* Pack generation works.
* SQLite validation works.
* Delta generation works.
* Versioning works.
* Manifest generation works.
* Checksums work.
* Atomic release works.
* Rollback works.
* Scheduling works.
* Checkpoint recovery works.
* Observability works.
* Security controls are implemented.
* Tests pass.
* Deployment works.
* The implementation matrix contains no unresolved required items.

---

# 29. IMPORTANT SYSTEM BOUNDARY

This system must remain within the responsibilities defined by the specifications.

Do not accidentally implement unrelated systems.

In particular, unless explicitly required by the specifications:

```text
DO NOT IMPLEMENT

Mobile UI
Mobile Map Rendering
Organic Maps Rendering
Mobile Navigation
Realtime Chat
Realtime Presence
Realtime Messaging
FOMO Feed
Camera Engine
Mobile Authentication
Mobile Profile UI
```

The Venue Pack Engine produces venue intelligence and Country Pack infrastructure consumed by other FOMO systems.

---

# 30. WORKING RULE

Do not ask the developer to manually connect modules that the specifications already require to work together.

You are responsible for integrating the system.

Do not say:

> "The remaining integration can be completed later."

Do not say:

> "This is a placeholder."

Do not say:

> "This would require another developer."

Instead:

```text
IDENTIFY
↓
IMPLEMENT
↓
INTEGRATE
↓
TEST
↓
VERIFY
```

If a requirement is genuinely blocked by an unavailable external credential or external service, implement everything possible around that dependency, clearly identify the external blocker, and provide the exact configuration required to activate it.

---

# FINAL COMMAND

Now operate as the **production implementation agent**.

Start by recursively reading:

```text
D:\Android-fomo-main\docs\fomo_specs\internal Venue Pack Engine
```

Do not begin coding until you understand the specification set.

Then:

```text
1. Inventory the specifications
2. Build the architecture map
3. Build the implementation matrix
4. Inspect the existing repository
5. Map existing code to the specifications
6. Identify missing functionality
7. Identify incorrect functionality
8. Identify architectural conflicts
9. Implement the missing system
10. Integrate existing components
11. Run tests continuously
12. Perform production-readiness loops
13. Verify against every specification
14. Fix every discovered gap
15. Perform final end-to-end verification
```

**Do not merely tell me what needs to be built. Build it.**

**Do not merely generate a plan. Execute the plan.**

**Do not stop at scaffolding.**

**Do not stop at compilation.**

**Do not stop at unit tests.**

**Continue until the implementation is genuinely production-ready and demonstrably compliant with the complete specification set.**

---

### One important change from your original prompt

I would **not hard-code the detailed architecture into the prompt as the ultimate authority**.

Instead, make the hierarchy:

```text
                    MASTER SPEC FOLDER
                           │
                           ▼
                AUTHORITATIVE REQUIREMENTS
                           │
                           ▼
                 IMPLEMENTATION AGENT
                           │
             ┌─────────────┴─────────────┐
             ▼                           ▼
       EXISTING CODE                NEW CODE
             │                           │
             └─────────────┬─────────────┘
                           ▼
                  INTEGRATED SYSTEM
                           │
                           ▼
              TEST + VERIFY + REPEAT
```

